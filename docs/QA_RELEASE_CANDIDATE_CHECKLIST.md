# Release Candidate 実機テストチェックリスト

**作成日**: 2025-12-19
**対象ビルド**: Build ID `e7ef931c-1546-4149-95cc-e7fa50d04a32`
**対象Update**: Update Group ID `04347cf4-9db1-42ac-90b3-e9358c2a7279`

---

## 0. 前提（超重要）

### ビルド確認

- [ ] 新APK世代（Build ID: `e7ef931c-...`）が端末にインストールされている
  - ※EAS Updateだけだとネイティブ世代の差分でハマるので、まずここ固定
  - APKダウンロード: https://expo.dev/accounts/bme_llc/projects/mc-gate/builds/e7ef931c-1546-4149-95cc-e7fa50d04a32

### Update確認

- [ ] Update Group ID: `04347cf4-...` が当たっていること（設定画面のアプリ情報で確認）

---

## ① OAuthガード（起動時の戻り事故潰し）

**目的**: tokenがある限り index（ログイン）に落ちない

### テスト1: 正常ログイン→再起動

1. [ ] OAuthログイン成功 → Home画面表示
2. [ ] アプリを完全終了（kill）
3. [ ] 再起動
4. **✅ 期待**: ログイン画面を経由せず home に入る（チラつきなしが理想）

### テスト2: オフライン復帰

1. [ ] 端末を機内モード → 再起動
2. **✅ 期待**: tokenが残ってれば home（API失敗は出てもログイン要求はしない）

### テスト3: ログアウト→再起動

1. [ ] ログアウト（設定画面から）
2. [ ] kill → 起動
3. **✅ 期待**: ログイン画面に落ちる（当たり前の動作確認）

### 落ちた場合の原因候補

- `restoreSession()` が実は呼ばれてない / token保存先が違う
- `isAuthenticated` 判定が token有無と一致してない

---

## ② QR/顔認証UX（画角・状態・誤検出）

**目的**: 切替でカメラが安定し、前モードの残骸が出ない

### テスト1: モード切り替え

1. [ ] authタブで QRモード → 10秒スキャン
2. [ ] 顔認証へ手動切替
3. **✅ 期待**: 画角が変なジャンプしない
4. **✅ 期待**: QRの枠/結果/解析状態が残らない

### テスト2: 連打テスト

1. [ ] QR ↔ 顔 を 連打で5回
2. **✅ 期待**: 落ちない、固まらない、プレビュー真っ黒にならない

### テスト3: バックグラウンド復帰

1. [ ] バックグラウンド（ホームボタン）→ 復帰
2. **✅ 期待**: 直前モード維持、カメラ復帰が安定

### ここが死ぬなら

- 強制再マウントの key が mode変更で変わってない
- Camera の permission/ライフサイクル処理不足

---

## ③ プロジェクト切り替え（今回の目玉）

**目的**: token roles→候補抽出→UI切替→アプリ全体フィルタ反映→永続

### OAuth環境（ロールあり）

**前提**: Keycloak側で `project:PRJ001`, `project:PRJ002` を付与

#### テスト1: プロジェクト一覧表示

1. [ ] ログイン → 設定 → プロジェクト管理
2. **✅ 期待**: 切替ボタンが出る
3. **✅ 期待**: 候補が2件出る、選択中がバッジ表示

#### テスト2: プロジェクト切り替え

1. [ ] PRJ002へ切替
2. **✅ 期待**: 即時に `currentProject` が変わる
3. **✅ 期待**: Homeの統計/履歴がPRJ002にフィルタされる（useQueue連動確認）

#### テスト3: 永続化確認

1. [ ] アプリkill → 再起動
2. **✅ 期待**: 選択したプロジェクトが維持（永続化できてるか）

#### テスト4: ロールなしユーザー

1. [ ] ロールが `project:` 0件のユーザーでログイン
2. **✅ 期待**: `availableProjects` 空、切替UIは出ない（仕様通り）

### Mock環境（ロール無し）

1. [ ] Mock認証でログイン
2. **✅ 期待**: 切替ボタンは出ない（混乱防止として正しい）

---

## 追加で必須の"事故想定テスト"（5分で価値が高い）

### テスト1: Token期限切れの挙動

1. [ ] 期限切れ → API 401 → リフレッシュ無いならログイン誘導が出る、の一貫性

### テスト2: プロジェクト切替直後のAPI

1. [ ] 直後に古いproject_idでリクエスト飛ばないか（キャッシュ/Hook依存）

### テスト3: ロール名の揺れ

1. [ ] `project:PRJ001` 以外（`Project:` とか）を想定するなら、Keycloak規約を固定しとくべき
   - 今の実装は `startsWith("project:")` で厳密

---

## テスト結果報告フォーマット

### 成功時

```
✅ Release Candidate判定：OK

テスト端末：
- Android: [機種名] Android [バージョン]
- iOS: [機種名] iOS [バージョン]（未実施の場合は"N/A"）

テスト実施日時：2025-XX-XX XX:XX

全チェック項目：PASS
```

### 失敗時

```
❌ Release Candidate判定：NG

失敗ケース：
- [テスト項目番号] [テスト名]

再現手順：
1. [手順1]
2. [手順2]
...

期待結果：[期待される動作]
実際の結果：[実際に起きた動作]

ログ/スクリーンショット：
[添付または記載]

テスト端末：
- [機種名] Android [バージョン]
```

---

## 次のアクション

1. 上のチェックリストを Android 1台 / iOS 1台で実行
2. 失敗が1つでも出たら、失敗ケースだけログ（スクショ＋再現手順）を報告
3. 問題ゼロなら **「Release Candidate判定：OK」** で次の要求（RFPギャップ or 次機能）へ移動

---

**最終更新**: 2025-12-19
**作成者**: Claude Code
**承認**: 未承認（テスト実施待ち）
